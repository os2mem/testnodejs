extends layout.pug

block content
  div.error
    h1 Error Nro #{error.status}:
    h2= error.statusText
    a.button.show(href="/") Inicio
    .item
      pre #{error.stack}
    img(src="indentation-war.png")